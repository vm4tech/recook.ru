type CardRecookType = {
    id?: string,
    photoUrl: string,
    cookTime: number,
    rating: number,
    avatarUrl?: string,
    nowCooking?: number,
    genres?: string[],
    isPopular?: boolean,
    name: string
}