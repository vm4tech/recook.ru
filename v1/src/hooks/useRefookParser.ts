// import parsers from "../main/utils/parser";
import { SendFunctionType } from "../types/common/CommonTypes";
// МБ сделать прокидывание функции отправки заранее, а потом просто вызывать нужные методы и передавать туда объекты
export const useRefookParser = (sendFunction: SendFunctionType) => {
  return () => {};
};
